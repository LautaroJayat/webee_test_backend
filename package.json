{
  "name": "backend_webee",
  "version": "1.0.0",
  "description": "backend package for the webee interviewing process",
  "main": "index.js",
  "scripts": {
    "start:dev": "export ENV_FILE=dev.env && nodemon index.js",
    "start": "export ENV_FILE=test.env && node index.js",
    "test": "export ENV_FILE=test.env && mocha tests/**/*.test.js"
  },
  "author": "Lautaro Jayat",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "helmet": "^4.0.0",
    "mocha": "^8.1.1",
    "mongoose": "^5.10.0",
    "supertest": "^4.0.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
